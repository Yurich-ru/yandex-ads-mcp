# Backlog

Что планируется, но ещё не делается. Граница с другими файлами:

- `snapshot.md` — что **в работе прямо сейчас**.
- `backlog.md` (этот файл) — что **будет потом**, но уже признано как работа.

Формат пункта: `- [ ] <id>: <заголовок> — <контекст в одну строку>`. Id произвольный (B-001 или слаг) — главное, чтобы на него можно было сослаться из коммита или snapshot.

## Next

<!-- Пусто. B-001 (стартер влит в master) и B-002 (исключения в .gitignore есть)
     закрыты к 2026-09-14 — см. git-историю. -->

## Soon

<!-- Известная работа на ближайшее время, ещё не детализирована. -->

## Later

<!-- Признано нужным, но не запланировано. -->

## Won't do

- Portainer-автодеплой (`deploy.yml`) — пакет запускается ассистентом локально по stdio, сервиса нет.
