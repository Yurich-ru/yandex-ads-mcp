# [2026-09-12 17:44] Быстрые ссылки комбинаторных объявлений

## Что сделано

- В `yd_ads_update` добавлен `responsive_ad.sitelink_set_id` и маршрутизация `ResponsiveAd` через
  обязательный для этого типа API v501.
- `yd_ads_get` переведён на v501 и возвращает `SitelinkSetId` комбинаторного объявления.
- Добавлена офлайн-проверка payload `ResponsiveAd.SitelinkSetId`.
- Обновлены README и changelog.

## Зачем

Direct автоматически преобразует новые текстовые объявления в `RESPONSIVE_AD`. Переданный в
устаревшем `TextAd` набор быстрых ссылок при этом не применяется, поэтому его нужно прикреплять
через структуру `ResponsiveAd`.

## Обновлено

- [ ] agent_docs/architecture.md (не требуется)
- [ ] agent_docs/adr/ (не требуется)
- [x] Тесты
- [x] Документация

## Связанные решения

- Нет.

## Следующие шаги

- Нет.
