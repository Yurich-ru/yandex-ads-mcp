# [2026-09-11 17:14] Поддержка комбинаторных объявлений

## Что сделано

- `yd_ads_get` запрашивает поля `ResponsiveAd`, включая все заголовки и тексты.
- `yd_ads_update` принимает полный набор ассетов комбинаторного объявления.
- Добавлены офлайн-проверки схемы инструмента и запросов к Direct API.

## Зачем

Обновление комбинаторного объявления через устаревший `TextAd` теряло дополнительные заголовки и
возвращало предупреждение Direct API 10252. Новый контракт напрямую использует `ResponsiveAd`.

## Обновлено

- [ ] agent_docs/architecture.md (не требуется: границы компонентов не изменились)
- [ ] agent_docs/adr/ (не требуется: локальное расширение существующего инструмента)
- [x] Тесты
- [x] Документация

## Связанные решения

- Официальный контракт Direct API: `Ads.get` и `Ads.update` для `ResponsiveAd`.

## Следующие шаги

- После принятия PR выпустить новую версию пакета.
